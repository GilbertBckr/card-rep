package com.cardrep.domain.model;

/**
 * Represents the difficulty rating a user assigns to a card after reviewing it.
 * Value Object (enum) - immutable, identity-less.
 */
public enum Difficulty {
    EASY,
    MEDIUM,
    HARD,
    AGAIN
}
